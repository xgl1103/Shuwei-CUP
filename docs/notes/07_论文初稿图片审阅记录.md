# 论文初稿图片审阅记录

审阅文件：

```text
E:\Code\数维杯\B题论文_基于可解释主题原型与风险优先级的多源异构文件智能治理模型1(1).docx
```

审阅时间：2026-05-10

## 1. 审阅方式

本次检查做了两类审阅：

1. 结构审计：解包 docx，检查内嵌图片数量、图片关系、图题和正文引用。
2. 页面审阅：使用 Word COM 将 docx 导出为 PDF，再渲染为页面图片，检查图片位置、图题顺序和页面效果。

由于本机未检测到 LibreOffice / soffice，未使用 Documents skill 默认 LibreOffice 渲染器；本次改用 Microsoft Word 导出 PDF 完成页面级检查。

临时审阅材料位于：

```text
E:\Code\数维杯\b_solution\tmp\paper_review
```

## 2. 总体结论

文档中实际内嵌了 13 张图片，并配有 13 个图题。从结构上看，不是图片对象全部丢失，而是存在三类问题：

1. 图号缺失：图9之后直接跳到图12，缺少图10和图11。
2. 图片版本错配：图1没有使用当前最终版“多源异构文件智能治理模型技术路线”；图15也没有使用真实人工核查平台截图。
3. 可读性不足：所有图片统一缩放到约 5.21in x 2.71in，宽幅流程图和平台图中的文字偏小，论文打印或评审阅读时可读性较弱。

## 3. 当前文档内图片清单

| 图号 | 当前图题 | 当前状态 | 审阅意见 |
|---|---|---|---|
| 图1 | 总体技术路线图 | 有图，但版本不对 | 应替换为 `E:\Code\数维杯\论文图片\ig_0b7b8a4fbe8888be0169ffb501dd0c819185427a3222141692.png` |
| 图2 | 三个问题承接关系图 | 有图 | 可保留，但与图1功能略重复，可考虑弱化或放到问题分析章节 |
| 图3 | 数据预处理流程图 | 有图 | 基本可用 |
| 图4 | 多格式文件解析示意图 | 有图 | 基本可用 |
| 图5 | 问题一主题发现流程图 | 有图 | 基本可用 |
| 图6 | k值敏感性与消融实验 | 有图 | 数据图正确，建议保留 |
| 图7 | 主题原型与主题族归并示意图 | 有图 | 基本可用，但可读性略弱 |
| 图8 | 问题二分类结果结构 | 有图 | 数据图正确，建议保留 |
| 图9 | 高频主题与教育科研细分分布 | 有图 | 数据图正确，建议保留 |
| 图10 | 缺失 | 无正文引用、无图题、无图片 | 建议补“字段加权相似度模型示意图” |
| 图11 | 缺失 | 无正文引用、无图题、无图片 | 建议补“置信度/边界状态/风险传递示意图”或“问题三复核指标构成图” |
| 图12 | AHP指标权重与一致性检验 | 有图 | 数据图正确，建议保留 |
| 图13 | 复核优先级分布 | 有图 | 数据图正确，建议保留 |
| 图14 | 资源约束分配结果 | 有图 | 数据图正确，建议保留 |
| 图15 | 人工核查平台与反馈闭环 | 有图，但版本不对 | 当前是示意图，应替换为 `E:\Code\数维杯\论文图片\人工核查平台界面截图.png` 或改名为“反馈闭环示意图” |

## 4. 具体问题

### 4.1 图1版本错误

当前 docx 中的图1是较早的九步式流程图，不是后续确定的最终总路线图。

应替换为：

```text
E:\Code\数维杯\论文图片\ig_0b7b8a4fbe8888be0169ffb501dd0c819185427a3222141692.png
```

替换后建议标题保持：

```text
图1 多源异构文件智能治理模型技术路线
```

### 4.2 图10和图11缺失

当前正文和图题中都没有图10、图11。图号从图9直接跳到图12。

建议补入：

```text
图10 字段加权相似度分类模型示意图
```

放置位置：第6章问题二，`6.3 字段加权相似度模型` 后。

用途：解释标题、正文、表格、OCR文本如何分别计算相似度，再通过字段权重融合到主题原型得分。

建议补入：

```text
图11 分类置信度与边界状态判别示意图
```

放置位置：第6章问题二，`6.4 置信度与边界样本判别` 或 `6.5 边界优化` 后。

用途：解释 assigned / multi_class / unclassifiable 的判别逻辑，以及这些边界状态如何传递到问题三复核优先级。

如果不想新增两张图，也可以删除图12、13、14、15的编号前移，改成连续的图10、图11、图12、图13。但更推荐补图，因为当前论文确实缺少对字段加权和边界判别机制的可视化解释。

### 4.3 图15不是平台截图

用户提供的真实平台截图位于：

```text
E:\Code\数维杯\论文图片\人工核查平台界面截图.png
```

当前 docx 中的图15是“人工核查平台与反馈闭环”的示意图，不是真实截图。若正文写“人工核查工作台”，应优先使用真实平台截图；若保留当前示意图，则图题应改成：

```text
图15 人工核查反馈闭环示意图
```

更合理的做法：

1. 在第8.4节放真实平台截图，图题为“人工核查平台界面”。
2. 如果还想保留闭环示意图，可以另放到方法章节或附录，不要替代平台截图。

### 4.4 图片整体缩放偏小

当前 13 张图片的显示尺寸基本都是：

```text
5.21in x 2.71in
```

该尺寸对数据图基本可接受，但对路线图、流程图、平台截图偏小。建议：

1. 宽幅路线图使用页面最大正文宽度，必要时单独横向页。
2. 平台截图建议裁剪掉多余空白，只保留关键区域：顶部指标、待复核队列、资源场景摘要。
3. 数据图保持当前宽度即可，但要确认坐标轴和图例打印后可读。

## 5. 推荐修正顺序

1. 替换图1为最终总路线图。
2. 在第6章补图10：字段加权相似度模型示意图。
3. 在第6章补图11：置信度/边界状态判别示意图。
4. 替换图15为真实人工核查平台截图。
5. 重新编号所有图，并同步正文引用和目录。
6. 导出 PDF 后逐页检查，确认图题与图片在同页或相邻位置，避免图题和图片分页断开。

## 6. 当前不需要优先处理的问题

以下图片目前可以先保留：

```text
图6 k值敏感性与消融实验
图8 问题二分类结果结构
图9 高频主题与教育科研细分分布
图12 AHP指标权重与一致性检验
图13 复核优先级分布
图14 资源约束分配结果
```

这些图来自代码生成结果，数据准确性比风格化图片更重要，建议作为论文中的核心结果图保留。
